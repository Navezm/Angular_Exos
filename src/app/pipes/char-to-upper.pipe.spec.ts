import { CharToUpperPipe } from './char-to-upper.pipe';

describe('CharToUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new CharToUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
