import { MatPaginationPipe } from './mat-pagination.pipe';

describe('MatPaginationPipe', () => {
  it('create an instance', () => {
    const pipe = new MatPaginationPipe();
    expect(pipe).toBeTruthy();
  });
});
